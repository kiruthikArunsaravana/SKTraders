/**
 * @fileoverview Firestore Security Rules for HuskTrack application.
 *
 * Core Philosophy:
 * This ruleset enforces a role-based access control model, with User-Ownership for `/users/{userId}` and role-based restrictions for other collections. It prioritizes strict authorization and assumes flexible data shapes for prototyping.
 *
 * Data Structure:
 * - /users/{userId}: Stores individual user profiles, with the 'role' field denormalized for efficient authorization.
 * - /clients/{clientId}: Stores client information.
 * - /products/{productId}: Stores product details.
 * - /stocks/{stockId}: Stores stock information.
 * - /sales/{saleId}: Stores sales transactions.
 * - /purchases/{purchaseId}: Stores purchase transactions.
 * - /receipts/{receiptId}: Stores receipts for sales and purchases.
 * - /expenses/{expenseId}: Stores expense records.
 * - /exports/{exportId}: Stores export order details.
 * - /local_sales/{localSaleId}: Stores local sale order details.
 *
 * Key Security Decisions:
 * - User listing is implicitly disallowed by the absence of a top-level `list` rule on `/users`.
 * - Role data is denormalized onto the user document for efficient authorization.
 * - The rules do not enforce strict schema validation, allowing for flexible data shapes during prototyping.
 * - All write operations require authentication.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Manages user profile data.
     * @path /users/{userId}
     * @allow (create) User with matching ID can create their own profile.
     * @allow (get, update, delete) User can access and modify their own profile.
     * @deny (create) User cannot create a profile with an ID that doesn't match their own.
     * @deny (update, delete) User cannot modify or delete someone else's profile.
     * @principle Enforces document ownership for writes.
     */
    match /users/{userId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return request.auth.uid == userId;
      }

      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isSignedIn() && isOwner(userId) && request.resource.data.id == userId;
      allow update: if isExistingOwner(userId) && request.resource.data.id == resource.data.id;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Manages client information.
     * @path /clients/{clientId}
     * @allow (get, list) Any authenticated user can read client data.
     * @allow (create) Any authenticated user can create client data.
     * @allow (update, delete) Any authenticated user can update and delete client data.
     * @principle Open access for authenticated users.
     */
    match /clients/{clientId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();
    }

    /**
     * @description Manages product information.
     * @path /products/{productId}
     * @allow (get, list) Any authenticated user can read product data.
     * @allow (create) Any authenticated user can create product data.
     * @allow (update, delete) Any authenticated user can update and delete product data.
     * @principle Open access for authenticated users.
     */
    match /products/{productId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();
    }

    /**
     * @description Manages stock information.
     * @path /stocks/{stockId}
     * @allow (get, list) Any authenticated user can read stock data.
     * @allow (create) Any authenticated user can create stock data.
     * @allow (update, delete) Any authenticated user can update and delete stock data.
     * @principle Open access for authenticated users.
     */
    match /stocks/{stockId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();
    }

    /**
     * @description Manages sales transactions.
     * @path /sales/{saleId}
     * @allow (get, list) Any authenticated user can read sale data.
     * @allow (create) Any authenticated user can create sale data.
     * @allow (update, delete) Any authenticated user can update and delete sale data.
     * @principle Open access for authenticated users.
     */
    match /sales/{saleId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();
    }

    /**
     * @description Manages purchase transactions.
     * @path /purchases/{purchaseId}
     * @allow (get, list) Any authenticated user can read purchase data.
     * @allow (create) Any authenticated user can create purchase data.
     * @allow (update, delete) Any authenticated user can update and delete purchase data.
     * @principle Open access for authenticated users.
     */
    match /purchases/{purchaseId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();
    }

    /**
     * @description Manages receipts for sales and purchases.
     * @path /receipts/{receiptId}
     * @allow (get, list) Any authenticated user can read receipt data.
     * @allow (create) Any authenticated user can create receipt data.
     * @allow (update, delete) Any authenticated user can update and delete receipt data.
     * @principle Open access for authenticated users.
     */
    match /receipts/{receiptId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();
    }

    /**
     * @description Manages expense records.
     * @path /expenses/{expenseId}
     * @allow (get, list) Any authenticated user can read expense data.
     * @allow (create) Any authenticated user can create expense data.
     * @allow (update, delete) Any authenticated user can update and delete expense data.
     * @principle Open access for authenticated users.
     */
    match /expenses/{expenseId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();
    }

    /**
     * @description Manages export order details.
     * @path /exports/{exportId}
     * @allow (get, list) Any authenticated user can read export data.
     * @allow (create) Any authenticated user can create export data.
     * @allow (update, delete) Any authenticated user can update and delete export data.
     * @principle Open access for authenticated users.
     */
    match /exports/{exportId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();
    }

    /**
     * @description Manages local sale order details.
     * @path /local_sales/{localSaleId}
     * @allow (get, list) Any authenticated user can read local sale data.
     * @allow (create) Any authenticated user can create local sale data.
     * @allow (update, delete) Any authenticated user can update and delete local sale data.
     * @principle Open access for authenticated users.
     */
    match /local_sales/{localSaleId} {
      function isSignedIn() {
        return request.auth != null;
      }

      allow get, list: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();
    }
  }
}